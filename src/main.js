import $ from 'jquery';
import 'bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';
import './styles.css';

$(document).ready(function() {
  $("#submit").click(function() {
    console.log(here);
    class UserInput {
      constructor (userAge, userLifeExpectancy) {
        this.userAge = '#user-age';
        this.userLifeExpectancy = '#user-life-expectancy';
      }
    }

  });
});
